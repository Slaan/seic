<table>

	<colgroup>
	    <col span="1" style="width: 33%;">
		<col span="1" style="width: 33%;">
		<col span="1" style="width: 33%;">
	</colgroup>
	
	<tbody>

        <br>
		<br>
		<br>

		<% @groups.each do |group| %>
			<tr>
				<td><%= group.name %></td>
				<td><%= group.details %></td>
				<% unless @group_membership.include? group.id %>
					<td><%= link_to 'Join Group', join_group_path(group_id: group.id) %></td>
				<% end %>
			</tr>
		<% end %>
	</tbody>
	
</table>
